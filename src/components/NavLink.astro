---
const { name, path, isActive } = Astro.props;
---

<a
  class={`font-bold text-md hover:border-blue-600 hover:border-3 transition duration-500 ease-in-out p-2 rounded-2xl text-center ${isActive ? "bg-blue-600 text-white" : ""}`}
  href={path}
>
  {name}
</a>
