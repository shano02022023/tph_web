---
const { name, path, isActive } = Astro.props;
---

<a
  class={`font-bold text-md dark:text-gray-900 hover:border-blue-600 hover:border-3 transition duration-500 ease-in-out p-2 rounded-2xl text-center ${isActive ? "bg-blue-600 text-white dark:text-white" : ""}`}
  href={path}
>
  {name}
</a>
